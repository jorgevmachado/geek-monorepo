'use client';
import { Default } from '@geek/ui';

import { config } from '../config';

export default function Rental() {
    return (
        <Default {...config}>
            <h3> ALUGUEL </h3>
        </Default>
    );
}