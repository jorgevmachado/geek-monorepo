import React from 'react';

import './G{{ pascalCase name }}.scss';

interface G{{ pascalCase name }}Props {
  children: React.ReactNode;
}

export default function G{{ pascalCase name }}({ children }: G{{ pascalCase name }}Props) {
  return (
    <div className="g-{{ kebabCase name }}">
      <h1>{{ pascalCase name }} Component</h1>
      {children}
    </div>
  );
};
