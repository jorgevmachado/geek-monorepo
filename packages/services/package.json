{
  "name": "@geek/services",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --max-warnings 0 --fix",
    "build": "rimraf dist && tsc && tsc-alias -p tsconfig.json"
  },
  "devDependencies": {
    "@geek/typescript-config": "*",
    "@types/node": "^22.7.5",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.6.3"
  }
}
