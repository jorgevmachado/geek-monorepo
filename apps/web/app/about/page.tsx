'use client';
import { Default } from '@geek/ui';

import { config } from '../config';

export default function About() {
    return (
        <Default {...config}>
            <h1> About </h1>
        </Default>
    );
}